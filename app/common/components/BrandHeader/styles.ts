import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    display: 'flex',
    alignItems: 'center',
    columnGap: '6px',
  },
  title: {
    textTransform: 'uppercase',
    flexGrow: 1,
  },
});
