import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    fontWeight: 700,
    fontSize: '18px',
    textTransform: 'uppercase',
    letterSpacing: '-.05em',
  },
});
